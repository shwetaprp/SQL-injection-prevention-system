# SQL-injection-prevention-system
This project focuses on detecting and preventing data leaks caused by SQL injection attacks. SQL injection is a common security issue where attackers manipulate database queries to access sensitive data.  In this system, secure coding techniques like parameterized queries and input validation are used to protect the database. 
